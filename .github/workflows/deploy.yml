name: Deploy to Production

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v2.1.3, etc.

env:
  GO_VERSION: '1.23.x'
  BINARY_NAME: 'pico-api-go'
  DEPLOY_PATH: '/home/banuacod/domains/banuacoder.com/covid-api'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for proper version info

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ env.GO_VERSION }}

    - name: Get version from tag
      run: |
        echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
        echo "Deploying version: ${GITHUB_REF#refs/tags/}"

    - name: Build binary for Linux
      run: |
        echo "Building ${{ env.BINARY_NAME }} for Linux..."
        CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ${{ env.BINARY_NAME }} ./cmd/main.go
        ls -la ${{ env.BINARY_NAME }}
        
    - name: Verify binary
      run: |
        file ${{ env.BINARY_NAME }}
        echo "Binary size: $(du -h ${{ env.BINARY_NAME }} | cut -f1)"

    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        echo "${{ secrets.DEPLOY_HOST_KEY }}" > ~/.ssh/known_hosts
        chmod 644 ~/.ssh/known_hosts

    - name: Deploy to production server
      run: |
        echo "🚀 Starting deployment of ${{ env.VERSION }} to production..."
        
        # Upload the binary to a temporary location first
        echo "📤 Uploading binary..."
        scp ${{ env.BINARY_NAME }} ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/tmp/${{ env.BINARY_NAME }}-${{ env.VERSION }}
        
        # Execute deployment script on remote server
        ssh ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} << 'EOF'
          set -e
          
          DEPLOY_PATH="${{ env.DEPLOY_PATH }}"
          BINARY_NAME="${{ env.BINARY_NAME }}"
          VERSION="${{ env.VERSION }}"
          TEMP_BINARY="/tmp/${BINARY_NAME}-${VERSION}"
          
          echo "🔍 Checking deployment environment..."
          cd "$DEPLOY_PATH"
          pwd
          ls -la
          
          echo "⏹️  Stopping application..."
          if [ -f "./stop.sh" ]; then
            ./stop.sh
            echo "✅ Application stopped successfully"
          else
            echo "⚠️  stop.sh not found, skipping stop step"
          fi
          
          echo "💾 Backing up current binary..."
          if [ -f "$BINARY_NAME" ]; then
            cp "$BINARY_NAME" "${BINARY_NAME}.backup.$(date +%Y%m%d_%H%M%S)"
            echo "✅ Backup created"
          fi
          
          echo "🔄 Replacing binary..."
          mv "$TEMP_BINARY" "$BINARY_NAME"
          chmod +x "$BINARY_NAME"
          echo "✅ Binary replaced and made executable"
          
          echo "🚀 Starting application..."
          if [ -f "./start.sh" ]; then
            ./start.sh
            echo "✅ Application started successfully"
          else
            echo "⚠️  start.sh not found, skipping start step"
          fi
          
          echo "🎉 Deployment of ${VERSION} completed successfully!"
        EOF

    - name: Verify deployment
      run: |
        echo "🔍 Verifying deployment..."
        sleep 5  # Give the service a moment to start
        
        # Check if the service is responding (adjust URL as needed)
        if curl -f -s https://pico-api.banuacoder.com/health > /dev/null; then
          echo "✅ Deployment verification successful - API is responding"
        else
          echo "⚠️  Deployment verification failed - API might not be responding yet"
          echo "Please check the logs manually"
        fi

    - name: Create deployment summary
      run: |
        echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "- **Version**: ${{ env.VERSION }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Binary**: ${{ env.BINARY_NAME }}" >> $GITHUB_STEP_SUMMARY  
        echo "- **Target**: ${{ env.DEPLOY_PATH }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Status**: ✅ Deployed successfully" >> $GITHUB_STEP_SUMMARY
        echo "- **API URL**: https://pico-api.banuacoder.com" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
        echo "1. Verify API functionality at https://pico-api.banuacoder.com/health" >> $GITHUB_STEP_SUMMARY
        echo "2. Monitor logs for any issues" >> $GITHUB_STEP_SUMMARY
        echo "3. Update documentation if needed" >> $GITHUB_STEP_SUMMARY

  notification:
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: always()
    
    steps:
    - name: Notify deployment status
      run: |
        if [ "${{ needs.build-and-deploy.result }}" == "success" ]; then
          echo "✅ Deployment successful for ${{ github.ref_name }}"
        else
          echo "❌ Deployment failed for ${{ github.ref_name }}"
          exit 1
        fi